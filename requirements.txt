# Pinned to the versions the archived results in results/ were produced with.
# Reproduce with:  pip install -r requirements.txt
scikit-learn==1.8.0
numpy==2.4.4
pandas==3.0.2
scipy==1.17.1
matplotlib==3.10.8
pypdf==5.9.0  # only needed if you want to check the PDF page count
